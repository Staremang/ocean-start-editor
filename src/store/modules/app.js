const state = {
  printStatus: null
}

const getters = {}

const actions = {}

const mutations = {
  SET_PRINT_STATUS (state, status) {
    state.printStatus = status
  }
}

export default {
  state,
  getters,
  actions,
  mutations
}
